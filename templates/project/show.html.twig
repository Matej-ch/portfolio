{% extends 'base.html.twig' %}

{% block title %} Project {% endblock %}

{% block body %}

<h1>{{ question }}</h1>

<div>
    Eventually this WILL BE awesome
</div>

<h4>Answers ({{ answers|length }})</h4>
<ul>
{% for answer in answers %}
    <li>{{ answer }}</li>
{% endfor %}
</ul>

    <a href="{{ path('app_homepage') }}">Home</a>

{% endblock %}