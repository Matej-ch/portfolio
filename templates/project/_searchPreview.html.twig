<div class="flex flex-col absolute">
    {% for project in projects %}
        <a href="{{ path('project_show',{slug:project.slug}) }}" class="py-2">
            {{ project.name }}
        </a>
    {% else %}
        <div class="pt-2">
            No results found
        </div>
    {% endfor %}
</div>